class_name StatusEffects

## Compile-checked ids for the status effects in data/status_effects/.
## These are keys, not display text — resolve one through
## ContentDatabase.get_status_effect() to get its name, color, and behavior.

const BLIND = &"blind"
const POISON = &"poison"
const PARALYZE = &"paralyze"
const CHARM = &"charm"
